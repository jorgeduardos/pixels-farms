<!-- typekit preloader -->
{# Uncommend to use.
Example:
src="https://use.typekit.net/YOUR_TYPEKIT_CODE.js"
#}
{# <script src="https://use.typekit.net/ilo1vii.js"></script>
<script>
  try {
    Typekit.load({
      loading: function() {
        // JavaScript to execute when fonts start loading
        //console.log('Loading fonts');
        document.querySelector('html').classList.add('fonts-loading');
      },
      active: function() {
        // JavaScript to execute when fonts become active
        //console.log('Fonts have loaded');
        document.querySelector('html').classList.replace('fonts-loading', 'fonts-loaded');
      }
    })
  } catch(e) {
    // Add class 'fallback-fonts' to html tag
    document.querySelector('html').classList.replace('fonts-loading', 'fallback-fonts');
  }
</script> #}
<!-- typekit preloader -->
